import { extendTheme } from "@chakra-ui/react"
export default extendTheme({
  styles: {
    global: {
      body: {
        bg: "gray.800",
        color: "white",
        fontFamily: "'Inter', sans-serif",
        fontSize: "15px",
      },
    },
  },
})
